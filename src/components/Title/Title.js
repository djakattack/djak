import React from 'react'

// "Coder" "Artist" and "Scientist" will link to different parts of the profile.

const Title = ({ props }) => (
    <span>
        <strong><span className="title-name">Djak</span></strong> is a coder, artist, and scientist.
    </span>
)

export default Title